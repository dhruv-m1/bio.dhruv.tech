---
// Plays a lottie animation.

const {autoplay = 'true', loop = 'true', mode = 'normal', src} = Astro.props;
---

<dotlottie-player
    autoplay={autoplay}
    loop={loop}
    mode={mode}
    src={`${src}.lottie`}
>
</dotlottie-player>